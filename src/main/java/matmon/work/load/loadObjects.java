package matmon.work.load;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public interface loadObjects<T> {

    static final Logger logger = LogManager.getLogger(loadObjects.class);

}
